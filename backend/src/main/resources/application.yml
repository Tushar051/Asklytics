spring:
  application:
    name: asklytics-backend
  
  # MongoDB Configuration
  data:
    mongodb:
      host: localhost
      port: 27017
      database: asklytics
      auto-index-creation: true
  
  # Email Configuration for OTP
  mail:
    host: smtp.gmail.com
    port: 587
    username: 'tusharkedar110@gmail.com'
    password: 'jhyj tyyd zgtl hbtb'
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  
  
  # Jackson Configuration
  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false

# JWT Configuration
jwt:
    secret: "4NV3lTBRRGY5rb8AA36j2mYKD6SoTtWfVRLsrpuMLN7Wjp07M13yTExdtB4xV7pJBP/ghsn3eRMQVPiIOeORkg=="
    expiration: 86400000
    refresh-expiration: 604800000


# Gemini Configuration
gemini:
  api-key: ${GEMINI_API_KEY:}
  model: gemini-pro
  max-tokens: 1000

# Server Configuration
server:
  port: 8080
  servlet:
    context-path: /api

# Logging Configuration
logging:
  level:
    com.asklytics: DEBUG
    org.springframework.security: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

# CORS Configuration
cors:
  allowed-origins: "http://localhost:3000,http://localhost:3001"
  allowed-methods: "GET,POST,PUT,DELETE,OPTIONS"
  allowed-headers: "*"
  allow-credentials: true 